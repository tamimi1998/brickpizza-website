/**
 * Re-export performance API from the provider.
 * Quality is derived once on mount + prefers-reduced-motion.
 */
export { usePerformanceContext as usePerformanceSettings } from "@/components/PerformanceProvider";
